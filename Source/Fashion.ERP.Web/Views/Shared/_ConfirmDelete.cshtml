@* * * * * * * * * * * * * * * * * * * * * * **
** Popup modal para confirmação de exclusão
**
** Sempre adicionar essa partial fora do form.
** * * * * * * * * * * * * * * * * * * * * * *@
<div id="confirm-delete" class="modal hide fade">
    <div class="modal-header">
        <a href="javascript:void(0)" class="close" data-dismiss="modal" aria-hidden="true">&times;</a>
        <h3>Excluir</h3>
    </div>
    <div class="modal-body">
        <p>Você deseja realmente excluir o item?</p>
    </div>
    <div class="modal-footer">
        <form id="modal-form" method="POST">
            @Html.AntiForgeryToken()
            <button type="button" class="btn" data-dismiss="modal" aria-hidden="true">Cancelar</button>
            <button class="btn btn-primary btn-danger" type="submit" data-loading-text="Aguarde...">Excluir</button>
        </form>
    </div>
</div>
<script>
    $(document).ready(function () {
        $('.delete').on('click', function (ev) {
            var href = $(this).attr('href');
            $('#modal-form').attr('action', href);
            $('#confirm-delete').modal('show');
            return false;
        });
    });
</script>
