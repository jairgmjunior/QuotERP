@using Fashion.Framework.Common.Utils
@model Enum

@{
    var display = Model.GetDisplay();
    var displayText = display != null ? display.Name.ToUpper() : Model.ToString().ToUpper();
}

@Html.Label(string.Empty, displayText.ToUpper(), new { @class = "label"})