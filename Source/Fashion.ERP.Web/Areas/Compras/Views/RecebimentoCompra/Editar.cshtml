@model Fashion.ERP.Web.Areas.Compras.Models.RecebimentoCompraModel
@using (Html.BeginForm(MVC.Compras.RecebimentoCompra.Editar(), FormMethod.Post, new { id="form", @class = "form-horizontal" })) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummaryEx()
	@Html.HiddenFor(x => x.Id)
    @Html.Partial("_NovoOuEditar", Model)
}
@{
    Html.RenderAction("PesquisarComParametros", "Fornecedor", new { area = "Comum", SelecaoUnica = "true" });
}

@{Html.RenderPartial("_ConfirmDelete");}
<script>

    function obtenhaUrlActionSubmit() {
        return '@Url.Action("Editar", "RecebimentoCompra")';
    }

    $(document).ready(function () {

        $("#Data").data("kendoDatePicker").enable(false);
        $("#Numero").attr("disabled", "disabled");
        
        $("#Fornecedor").data("kendoComboBox").enable(false);
        $('#pesquisar-fornecedor').prop("disabled", true);

        $("#Unidade").prop("disabled", true);

        $("#DepositoMaterial").prop("disabled", true);
    });
</script>    