@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridBarraModel>
<div class="well well-sm">
    @Html.ActionLinkAuth("Novo", MVC.Comum.Barra.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
    .Name("Grid")
    .Columns(columns =>
    {
        columns.Bound(x => x.Descricao);
        columns.Template(item => Html.EditarAuth(MVC.Comum.Barra.Editar(item.Id))).Width(80);
        columns.Template(item => Html.EditarSituacaoAuth(MVC.Comum.Barra.EditarSituacao(item.Id), item.Ativo)).Width(80);
    })
)