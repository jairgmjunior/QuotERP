@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridVariacaoModel>
<div class="well well-sm">
    @Html.ActionLinkAuth("Novo", MVC.Comum.Variacao.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
	.Name("Grid")
	.Columns(columns =>
	{
		columns.Bound(x => x.Nome);
        columns.Template(item => Html.EditarAuth(MVC.Comum.Variacao.Editar(item.Id))).Width(80);
        columns.Template(item => Html.EditarSituacaoAuth(MVC.Comum.Variacao.EditarSituacao(item.Id), item.Ativo)).Width(80);
	})
)
