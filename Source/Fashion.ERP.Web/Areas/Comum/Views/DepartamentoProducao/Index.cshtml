@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridDepartamentoProducaoModel>
<div class="well well-sm">
    @Html.ActionLinkAuth("Novo", MVC.Comum.DepartamentoProducao.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
	.Name("Grid")
	.Columns(columns =>
	{
		columns.Bound(x => x.Nome);
		columns.Bound(x => x.Criacao);
		columns.Bound(x => x.Producao);
        columns.Template(item => Html.EditarAuth(MVC.Comum.DepartamentoProducao.Editar(item.Id))).Width(80);
        columns.Template(item => Html.EditarSituacaoAuth(MVC.Comum.DepartamentoProducao.EditarSituacao(item.Id), item.Ativo)).Width(80);
	})
)