@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridClassificacaoModel>
<div class="well well-sm">
    @Html.ActionLinkAuth("Novo", MVC.Comum.Classificacao.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
    .Name("Grid")
    .Columns(columns =>
    {
        columns.Bound(x => x.Descricao);
        columns.Template(item => Html.EditarAuth(MVC.Comum.Classificacao.Editar(item.Id))).Width(80);
        columns.Template(item => Html.EditarSituacaoAuth(MVC.Comum.Classificacao.EditarSituacao(item.Id), item.Ativo)).Width(80);
    })
)