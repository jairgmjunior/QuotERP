@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridCentroCustoModel>
<div class="well well-sm">
    @Html.ActionLinkAuth("Novo", MVC.Comum.CentroCusto.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
	.Name("Grid")
	.Columns(columns =>
	{
		columns.Bound(x => x.Codigo);
		columns.Bound(x => x.Nome);
		columns.Template(item => Html.EditarAuth(MVC.Comum.CentroCusto.Editar(item.Id))).Width(80);
        columns.Template(item => Html.EditarSituacaoAuth(MVC.Comum.CentroCusto.EditarSituacao(item.Id), item.Ativo)).Width(80);
	})
)
