@model Fashion.ERP.Web.Areas.Comum.Models.TipoFornecedorModel

@using (Html.BeginForm(MVC.Comum.TipoFornecedor.Editar(), FormMethod.Post, new { id="form", @class = "form-horizontal" })) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummaryEx()
    @Html.Partial("_NovoOuEditar", Model)
}
@{ Html.RenderPartial("_ConfirmDelete"); }