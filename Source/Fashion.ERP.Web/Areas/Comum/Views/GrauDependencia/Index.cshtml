@model IEnumerable<Fashion.ERP.Web.Areas.Comum.Models.GridGrauDependenciaModel>

<div class="well well-small">
    @Html.ActionLinkAuth("Novo", MVC.Comum.GrauDependencia.Novo(), new { @class = "btn btn-primary" })
</div>
@Html.ValidationSummaryEx()
@(Html.Kendo().Grid(Model)
    .Name("Grid")
    .Columns(columns =>
    {
        columns.Bound(x => x.Descricao);
        columns.Template(item => Html.EditarAuth(MVC.Comum.GrauDependencia.Editar(item.Id))).Width(80);
    })
)