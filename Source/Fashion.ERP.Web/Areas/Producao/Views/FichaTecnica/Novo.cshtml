@model Fashion.ERP.Web.Areas.Producao.Models.FichaTecnicaModel
@using (Html.BeginForm(MVC.Producao.FichaTecnica.Novo(), FormMethod.Post, new { id="form", @class = "form-horizontal" })) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummaryEx()
    @Html.Partial("_NovoOuEditar", Model)
}