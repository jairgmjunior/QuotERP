@model Fashion.ERP.Web.Areas.Producao.Models.RemessaProducaoModel

@using (Html.BeginForm(MVC.Producao.RemessaProducao.Editar(), FormMethod.Post, new { id="form", @class = "form-horizontal" })) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummaryEx()
    @Html.Partial("_NovoOuEditar", Model)
}

@{
    Html.RenderPartial("_ConfirmDelete");
}

<script>
    $(document).ready(function () {
        $("#Numero").attr("disabled", "disabled");
        $('#CapacidadeRemessa').attr('readonly', true);
    });
</script>
