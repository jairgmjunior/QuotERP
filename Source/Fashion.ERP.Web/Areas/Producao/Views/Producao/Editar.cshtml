@model Fashion.ERP.Web.Areas.Producao.Models.ProducaoModel

@using (Html.BeginForm(MVC.Producao.Producao.Editar(), FormMethod.Post, new { id="form", @class = "form-horizontal" })) {
	@Html.AntiForgeryToken()
    @Html.ValidationSummaryEx()
    @Html.Partial("_NovoOuEditar", Model)
}
@{Html.RenderPartial("_ConfirmDelete");}

@{
    Html.RenderAction("PesquisarVarios", "FichaTecnica", new { area = "Producao" });    
    Html.RenderAction("PesquisarComParametros", "Funcionario", new { area = "Comum", SelecaoUnica = "true" });
}

<script>
    $(document).ready(function () {
        $("#Data").data("kendoDatePicker").enable(false);
        $("#Numero").attr("disabled", "disabled");
        $("#SituacaooProducao").attr("disabled", "disabled");
    });
</script>
